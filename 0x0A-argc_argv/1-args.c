#include <stdio.h>
/**
 * main - function  entry point  prints the number of arguments passed into it.
 * @argc : number of arg
 * @argv : array of arg
 *
 * Return: void
 */

int main(int argc, char *argv[] __attribute__((unused)))
{
	printf("%s\n", argc);
	return (0);
}
